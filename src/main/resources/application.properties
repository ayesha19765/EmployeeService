spring.application.name=TestingApp

#Port configuration
server.port=8081
#  //port on which your application will be running

#DB configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/employeeDB
spring.datasource.username=
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#  // TO see all the sql statements runned by hibernate
spring.jpa.properties.hibernate.format_sql=true //Format the hibernate queries, statements
